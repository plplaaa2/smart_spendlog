# Changelog - Version 2.0.2

본 문서(`CHANGEME.md`)는 배포된 상용 버전 `2.0.0` 이후 `dev` 브랜치를 거쳐 현재 `2.0.2` 버전에 적용된 핵심 변경점 및 신규 기능 목록을 명시합니다.

---

## 🚀 주요 변경 사항 & 신규 기능

### 1. ⚙️ AI 설정 분리 및 종속적 UI 제어 (마스터 스위치 도입)
*   **AI 기능 활성화 마스터 스위치 도입**: 전반적인 AI 기능 사용 제어를 위한 "AI 기능 활성화(마스터 스위치)"와 수신 알림 문자 분석 실패 시 폴백 파싱을 제어하는 "AI 자동 파싱 활성화" 스위치를 개별 분리하였습니다.
*   **종속적 UI 제어 및 가시성 개선**: 마스터 스위치(`settings-ai-enabled`)가 해제되면 서브 스위치(`settings-ai-parsing-enabled`)가 자동으로 비활성화(disabled) 및 체크 해제(unchecked)되며, 하위 AI 상세 옵션 입력 폼 컨테이너가 자동으로 숨겨지도록 유기적인 화면 반응 제어를 적용했습니다.
*   **API 검증 논리 세분화**:
    *   **자동 파싱 (Webhook)**: 알림 자동 파싱 시에는 마스터 스위치(`ai_enabled`)와 서브 스위치(`ai_parsing_enabled`)가 모두 활성화(true)되어 있어야 실행됩니다.
    *   **수동 생성 기능 (AI 소비 리포트 & AI 규칙 패턴 생성)**: 자동 파싱 스위치의 활성화 여부와 무관하게 마스터 스위치(`ai_enabled`)가 활성화되어 있으면 정상 동작하도록 검증 로직을 개선했습니다.

### 2. 🏷️ "외식비" 카테고리 명칭 변경 및 알림 파싱 엔진 고도화
*   **카테고리 직관성 개선**: 기존 식비 관련 카테고리(식비, 식비/외식 등)를 보다 명확하고 직관적인 **'외식비'**로 명칭을 전면 변경하고 관련 프리셋 데이터를 업데이트했습니다.
*   **알림 파싱 엔진 고도화**:
    *   문장 내 줄바꿈 문자 처리 방식을 UNIX 스타일(`\n`)로 표준화하여 정규식 규칙 매칭 성능을 개선했습니다.
    *   휴리스틱 파싱 알고리즘을 개선하여 금액, 사용처, 결제 방식의 오인식을 최소화했습니다.

### 3. 🤖 AI 지원 모델 최적화 및 안정성 강화
*   **모델 라인업 최적화**: 지원 모델 사양을 대폭 정리 및 최적화하여 OpenAI 모델군은 최신 초경량 및 소형 모델(`gpt-5.4-mini`, `gpt-5.4-nano`)에 집중하고, Gemini 모델군은 `gemini-3.1-flash-lite`를 최우선 순위로 사용하도록 설정하였습니다. 구형/미지원 모델 폴백 로직을 전면 제거하여 처리 속도와 일관성을 보장합니다.
*   **API Key 저장 및 검증 개선**: AI 설정 저장 및 불러오기 시 API Key 필드의 마스킹 갱신 로직을 보안 위협 없이 안정적으로 수행하도록 보완했습니다.
*   **ReferenceError 버그 해결**: AI 소비 리포트 생성 중 발생할 수 있는 내부 변수 참조 범위 오류(ReferenceError)를 수정하여 리포트 생성 프로세스의 안정성을 대폭 끌어올렸습니다.
